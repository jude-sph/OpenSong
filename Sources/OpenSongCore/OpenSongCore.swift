// OpenSongCore — engine library for the OpenSong macOS app.
// All library indexing, import/adopt, metadata/artwork resolution, transcoding,
// and device sync logic lives here so it can be tested headlessly (no GUI, no Xcode).

public enum OpenSongCore {
    public static let version = "0.1.0"
}
